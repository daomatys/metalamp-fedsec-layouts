include /components/date-picker/date-picker.pug

mixin cardDatePicker( item={} )
  .card-date-picker.expander__parent
    .card-date-picker__frame.adp-frame
    .card-date-picker__aim.expander__aim( data-dates=item.dates )
      +datePicker( item.id )