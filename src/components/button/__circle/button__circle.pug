include /components/material-icon-cell/material-icon-cell.pug

mixin circleButton( item )
  if ( item.text )
    .circle-button #{item.text}
  if ( item.icon )
    - const id = item.id + '_' + item.icon
    .circle-button
      +materialIcon( item.icon, id )
